# Create main.py file that imports functions from other files and orchestrates entire process of calculating efficient frontier, optimizing portfolio weights, executing trades, and backtesting strategy
# Use command-line arguments or environment variables to specify inputs and outputs of program
